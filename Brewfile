# Common: all machines (dev and prod)
brew "git"
# Local models for Aldine (headless formula, not the GUI cask)
brew "ollama"
# Open source tailscale + tailscaled, not the GUI cask: it runs as a system
# daemon at boot, and only this variant serves Tailscale SSH on macOS.
brew "tailscale"
